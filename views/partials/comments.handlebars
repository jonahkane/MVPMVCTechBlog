<div class="comments">
  {{#each this}}
    <section class="comment">
      <div>
        {{user.username}}
        on
        {{created_at}}
      </div>
      <div class="text">
        {{comment_text}}
      </div>
    </section>
  {{/each}}
</div>

{{! This code renders comment data and pins it to the blog ti is associated with. 
 it will show who created the comment, when, and will render the text of the comment as well }}